import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './components/App';
import './version';

ReactDOM.render(
  <App />,
  document.getElementById('askcharlie-app'), // eslint-disable-line no-undef
);
